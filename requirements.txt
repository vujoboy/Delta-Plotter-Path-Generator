# Pinned to the versions this app has been developed and tested against.
# Install with: pip install -r requirements.txt

numpy==2.2.6
opencv-python==4.13.0.88
shapely==2.1.2
scipy==1.17.1
matplotlib==3.10.8

# tkinter is part of the standard library on the official python.org
# Windows installer - nothing to install for it. If you're on a minimal/
# custom Python distribution that lacks it, reinstall Python from
# python.org with the default options (tkinter is included automatically).

# Only needed for building the .exe, not for running the app from source:
pyinstaller==6.22.2
